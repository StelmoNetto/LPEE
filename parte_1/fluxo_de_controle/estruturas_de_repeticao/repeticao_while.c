#include <stdio.h>

int main()
{
    int contador = 0; // Inicialização

    while (contador < 3) // Condição do Loop
    {  // Corpo de Loop
        printf("%d\n", contador);
        contador++; // Expressão de atualização
    }
    return 0;
}
