#include <stdio.h>

int main()
{
    for (int contador = 0; contador < 3; contador++)
        printf("%d\n", contador); // Corpo de loop

    for (contador = 0; contador < 3; contador++)
    {
        printf("%d\n", contador); // Corpo de loop
    }
    return 0;
}
